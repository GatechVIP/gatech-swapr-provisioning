version: "3.3"
services:
  db:
    image: postgres:9.6
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - db

  backend:
    build: ./gatech-swapr-server-node
    user: "node"
    volumes:
      - ./gatech-swapr-server-node:/data/app
      - ./config/backend-config.json:/data/app/config/config.json:ro
      - /data/node_modules
    working_dir: /data/app
    depends_on:
      - db
    networks:
      - backend
      - db

  www:
    image: nginx:latest
    depends_on:
      - backend
    networks:
      - backend
    volumes:
      - ./config/nginx.conf:/etc/nginx/conf.d/vhost.conf:ro
      - ./gatech-swapr-client/dist:/usr/share/nginx/html:ro
    links:
      - backend

networks:
  backend:
  db:

volumes:
  db-data:
