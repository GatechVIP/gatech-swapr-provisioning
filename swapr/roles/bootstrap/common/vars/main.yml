# file: bootstrap/common/vars/main.yml

# This determines which SSH keys in bootstrap/common/files/public_keys get added.
# These are filenames.
admin_public_key_filenames:
  - breardon6-1
  - sdouglas6-1
